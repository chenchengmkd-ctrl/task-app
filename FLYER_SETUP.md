# チラシ配り管理（flyer.html）セットアップ

チラシ配り（飛び込み営業）を最大3人で分担するための地図アプリです。
「どの建物を回ったか」を全員でリアルタイムに共有し、重複訪問を防ぎます。

既存のタスク管理アプリと同じ **GitHub Pages ＋ 同じ Supabase** をそのまま使うので、
新しいアカウントを作る必要はありません。**費用は一切かかりません。**

| 使うもの | 何のため | 料金 |
|---|---|---|
| OpenStreetMap + Leaflet | 地図の表示・建物の形 | 無料 |
| Overture Maps | 店舗・会社の名前（毎月更新） | 無料 |
| 国土地理院タイル | 淡色地図・航空写真 | 無料 |
| Supabase（今使っているもの） | ピンの保存・3人での共有 | 無料枠内 |
| GitHub Pages（今使っているもの） | 公開・HTTPS（GPSに必須） | 無料 |

**あなたの作業は STEP 1 と STEP 2 だけ**です。

---

## STEP 1. Supabase にテーブルを2つ作る（1回だけ）

1. [Supabase](https://supabase.com/dashboard) を開き、今使っているプロジェクト（`xldkfkhgazpugfuscpqt`）を選ぶ
2. 左メニューの **SQL Editor**（`</>` のアイコン）を開く
3. **New query** を押し、下をまるごと貼り付けて **RUN**（右下の緑ボタン）を押す

```sql
create table if not exists flyer_pins (
  id text primary key,
  lat double precision not null,
  lng double precision not null,
  status text not null default 'todo',
  label text default '',
  note text default '',
  author text default '',
  deleted boolean default false,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
create index if not exists idx_flyer_pins_updated on flyer_pins(updated_at);

create table if not exists flyer_settings (
  id text primary key,
  data jsonb default '{}'::jsonb,
  updated_at timestamptz default now()
);

alter table flyer_pins disable row level security;
alter table flyer_settings disable row level security;

-- 他のメンバーの画面に即座に反映させるための設定
do $$ begin
  alter publication supabase_realtime add table flyer_pins;
exception when others then null; end $$;
do $$ begin
  alter publication supabase_realtime add table flyer_settings;
exception when others then null; end $$;
```

4. 「Success. No rows returned」と出れば完了です。

---

## STEP 1b. 枚数の記録用に列を足す（1回だけ・2026-09-18 追加）

配った枚数と断られた件数を記録できるようにしたので、**もう一度だけ** SQL Editor で下を RUN してください。
実行するまで、画面の上に赤いお知らせが出て、枚数はその端末の中だけに残ります。

```sql
alter table flyer_pins add column if not exists sheets  integer default 0;
alter table flyer_pins add column if not exists refused integer default 0;
alter table flyer_pins add column if not exists done_at timestamptz;
```

---

## STEP 2. 公開する

このリポジトリに `flyer.html` を push するだけです。1〜2分で公開されます。

**https://chenchengmkd-ctrl.github.io/task-app/flyer.html**

このURLを、チラシ配りをする3人のスマホでブックマーク（ホーム画面に追加）してください。
タスク管理アプリの左上からも「📍 チラシ配り」で開けます。

> **GPSを使うのでHTTPSが必須です。** GitHub Pages は最初からHTTPSなのでそのまま使えます。

---

## 配る対象の区域（15か所）

配る対象として決めた町丁目を、**紫の線で地図に囲って**あります。

内神田一丁目／内神田二丁目／内神田三丁目／鍛冶町一丁目／鍛冶町二丁目／神田鍛冶町三丁目／
神田多町二丁目／神田司町二丁目／神田須田町一丁目／神田紺屋町／神田東紺屋町／神田北乗物町／
神田富山町／神田東松下町／神田美倉町

- 地図はこの15区域の外にはスクロールできません
- ピンを立てると、**どの町丁目かが自動で記録**されます
- ☰ の一覧で、**町丁目ごとの進捗**（配布済み ÷ 件数）が棒グラフで出ます
- 区域を足す・減らすときは、この手引きの一番下を見てください

## 最初に一度だけやること（アプリ内で）

1. アプリを開くと **名前** を聞かれるので入れる（ログインはありません。端末ごとに1回だけ）
2. （任意）お店の位置を決めると🏠マークが出ます。⚙ →「現在地にする」または「地図で選ぶ」

お店の位置は決めなくても使えます。決めた町丁目のほうが範囲の基準になっているためです。

---

## 使い方

### 印をつける（歩きながら）

1. 右下の青い「**＋ピン**」を押す → 追加モードになります
2. 下のバーで状況（**配布済み** など）を選ぶ
3. あとは **タップするだけ**。タップした場所によって動きが変わります
   - **建物の上** … その建物が**まるごと色で塗られます**
   - **建物がない場所**（道路・駐車場・登録のない一戸建てなど） … **ピンが立ちます**
4. 間違えたら下のバーの「**取消**」で直前の1件を取り消せます
5. 終わったら「**やめる**」で追加モードを抜けます

メモを書きながらつけたいときは、**長押し**してください（編集画面が一緒に開きます）。

塗った建物は、もう一度タップすれば状況を変えたり消したりできます。
建物名がOpenStreetMapに入っていれば、建物名欄に自動で入ります。

> 対象15区域の中にある **1744棟** の建物の形をアプリに入れてあります。
> 抜けている建物（特に小さな一戸建て）はピンでおぎなってください。

### 建物の名前を調べる

追加モードを**切った状態**で建物をタップすると、吹き出しが出ます。

- **町丁目**と、すでに印がついていれば**その状況**
- **その建物の名前**
- **その建物に入っているお店・会社**（近所の店は出しません）
  - 先頭に **会社 6 ・ 飲食 3** のような内訳が出るので、**入る前に配る先があるか分かります**
  - <span>会社</span>（青）／<span>飲食</span>（黄）／<span>店舗</span>（緑）／その他 で色分け。会社が上に来ます
- 一番下のボタンで、そのまま印をつけたり開いたりできます

電波がなくても出ます。

### ピンを直す

立っているピンをタップすると下から編集画面が出ます。

- **状況**：3つ。押した瞬間に保存され、他のメンバーにも反映されます
  | 状況 | 色 | 使うとき |
  |---|---|---|
  | **配布済み** | 緑 | 配れた |
  | **要再訪** | 青 | 留守・また来る |
  | **配れる所なし** | グレー | 住宅と飲食店だけで会社が無かった等、配る先が無かった |
- **建物名・部屋番号**：「〇〇ハイツ 101〜305」など
- **配った枚数**：− ＋ ＋5 のボタンで数えられます
- **断られた件数**：飛び込んで断られた数
- **メモ**：「管理人さんに許可もらった」「ポスト共用部のみ」など
- 記録日・最終更新の欄に **誰が・いつ** 触ったかが出ます

### 進捗を見る

画面の上に **今日 ○枚 ・ ○件 ・ 断り○** が常に出ます。現場で一番見たい数字です。

右上の **☰** に2つのタブがあります。

**場所ごと**
- 町丁目ごとの進捗（配布済み÷件数と枚数）。行をタップでその町丁目だけに絞り込み
- 1件ずつの記録。「地図で見る」でその場所へ飛べます

**日ごと**
- 一番上に **合計**（配った枚数／配れた件数／断られた件数）
- 日付ごとに **枚数・配れた・断り・配れず** と、**誰が何枚配ったか**
- 日付をタップすると、その日の記録が地図に映ります

> 日ごとの記録は、地図の印を消しても残ります。

### 現在位置

右下の **◎** ボタンで、自分の青い点に地図を寄せます。
初回はブラウザから位置情報の許可を聞かれるので「許可」を押してください。

### 次のラウンドを始める

右上の **⚙** →「**地図の印をすべて消す**」。
地図はまっさらになりますが、**「日ごと」の記録は残る**ので、これまでの枚数は後から確認できます。

---

## 3人での共有について

- 誰かがピンや建物の色を変えると、**他の人の画面に数秒で反映**されます
- 反映されないときでも、**12秒ごとに自動で取り直し**ます（画面を閉じて開き直しても最新になります）
- 同じピンを2人が同時に触った場合は、**あとから変更したほうが残ります**
- 電波が悪いところでピンを立てても消えません。画面右上が「**未送信 ○件**」になり、電波が戻ったら自動で送信されます

---

## こんなときは

| 症状 | 対処 |
|---|---|
| 右上に「**準備が必要**」と出る | STEP 1 のSQLがまだです。実行してから画面を再読み込みしてください |
| 右上に「**オフライン**」と出る | 電波が届いていません。記録は端末に残るので、そのまま続けて大丈夫です |
| 現在地が出ない | ブラウザの設定で位置情報が拒否されています。iPhoneは「設定 → Safari → 位置情報」、Androidは「Chrome → サイトの設定 → 位置情報」を許可に |
| 地図が見づらい／情報が古い | ⚙ →「地図の種類」で4種類から選べます。**標準地図**＝OpenStreetMap（お店の名前が多い）／**地理院地図**＝国の公式で建物1棟ずつが見える／**淡色地図**＝うすい色で目が疲れない／**航空写真**＝上から見た実物。どれもキー不要・無料です |
| エリアを広げたい／お店を移転した | ⚙ から「配る範囲の決め方」「お店の位置」をいつでも変更できます（全員に共有されます） |
| 紫の区域線が邪魔 | ⚙ →「町丁目の区域を地図に表示する」のチェックを外してください |
| 配る町丁目を変えたい | 私に町丁目の名前を伝えてください。境界線のデータを作り直して差し替えます |
| ピンを消したい | ピンをタップ →「削除」 |
| 建物の色を消したい | その建物をタップ →「削除」 |
| 塗りたい建物が地図に無い | その建物はOpenStreetMapに未登録です。ピンでおぎなってください |
| 上に赤いお知らせが出る | STEP 1b のSQLがまだです。実行して再読み込みしてください（タップで閉じられます） |
| つぶれた店の名前が出る | 店舗データを毎月更新の Overture Maps に入れ替えました（2026-08-19版）。それでも古いものが残っていたら、建物名欄に正しい名前を打てば3人全員に共有されます。⚙ の「建物の中のお店の名前を出す」を外して建物名だけにもできます |
| もっと拡大したい | 拡大の上限を上げました（地図の絵はぼやけますが、狭い建物もタップしやすくなります） |

---

## （参考）区域とお店のデータについて

どちらも **OpenStreetMap** から取り出して、`flyer.html` の中に直接書き込んであります。
現場で通信できなくても区域とお店が出るようにするためと、無料サービスに毎回問い合わせて
制限にかからないようにするためです。

- 区域の境界線 … `const AREAS` の部分（15区域）
- お店・会社の名前 … `const POIS` の部分（2615件・Overture Maps 2026-08-19版）
- 建物の形 … `const BUILDINGS` の部分（1744棟）

**町丁目を足す・減らすとき**、**お店の入れ替わりを反映したいとき**は、
この2か所を作り直す必要があります。町丁目の名前を伝えてもらえれば差し替えます。

### 店舗・会社データについて

`POIS` は **Overture Maps**（Meta と Microsoft が出資する無料の地図データ、毎月更新）の
2026-08-19 版から取り出しています。OpenStreetMap より新しく、**会社名が入っている**ので
飛び込み営業に向いています。

取り込むときに、次の2つを除いてあります。

1. **座標が1点に積み上がっているもの** … 住所から座標を出すのに失敗した件が1か所に71件たまっていた
2. **住所が神田・千代田でないもの** … 霞ケ関・池袋・横浜・大阪などの会社が誤って混ざっていた（230件）

それでも実際と合わない名前は残りえます。建物名欄に打ち直せば、そちらが3人に共有されます。
